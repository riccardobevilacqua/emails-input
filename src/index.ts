import { createEmailsInput } from './components/emails-input';
import './styles/styles.scss';

const inputContainerNode = document.querySelector('#emails-input');

createEmailsInput(inputContainerNode);
